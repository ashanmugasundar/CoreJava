package util;

public class FileHandling {

	/**
	 * Method to find whether a file exists or not 
	 * @param filename
	 * @return true if it finds the file or it returns false if file not exists.
	 */
	public boolean isFileExists(String filename) {
		System.out.println("File name to search is - " +filename);
		return true;
		
	}

}
